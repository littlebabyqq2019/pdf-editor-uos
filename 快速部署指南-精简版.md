# PDF编辑工具集成版 - 快速部署指南（精简版）

## ⭐ 精简版镜像优势

| 特性 | 精简版 | 原始版 | 优势 |
|------|--------|--------|------|
| **镜像大小** | 280MB | 1.32GB | 小 78.8% |
| **tar文件** | 267MB | 1.26GB | 小 79% |
| **传输时间** | 3-5分钟 | 15-20分钟 | 快 75% |
| **导入时间** | 1-2分钟 | 5-8分钟 | 快 70% |
| **功能** | ✅ 完整 | ✅ 完整 | 一致 |

---

## 🚀 快速部署（3步完成）

### 步骤1：传输文件到UOS系统

只需传输这两个文件：
```bash
pdf-editor-uos-lean-arm64.tar    # 267MB
docker-compose.yml                # 2KB
```

**传输命令（示例）：**
```bash
# 方式1：SCP
scp pdf-editor-uos-lean-arm64.tar docker-compose.yml user@uos-host:/home/user/

# 方式2：U盘
# 直接拷贝到U盘，然后在UOS系统上复制
```

---

### 步骤2：导入镜像

在UOS系统上执行：

```bash
# 导入镜像（1-2分钟）
docker load -i pdf-editor-uos-lean-arm64.tar

# 验证导入
docker images | grep pdf-editor-uos
# 预期输出：pdf-editor-uos   lean   73717c92ce1c   280MB
```

---

### 步骤3：启动应用

**方式A：使用docker-compose（推荐）**

```bash
# 启动
docker-compose up -d

# 查看日志
docker-compose logs -f

# 查看状态
docker-compose ps
```

**方式B：使用docker run**

```bash
# 创建数据目录
mkdir -p uploads processed

# 启动容器
docker run -d \
  --name pdf-editor \
  --platform linux/arm64 \
  -p 5000:5000 \
  -v $(pwd)/uploads:/app/uploads \
  -v $(pwd)/processed:/app/processed \
  --restart unless-stopped \
  pdf-editor-uos:lean

# 查看日志
docker logs -f pdf-editor
```

---

## 🌐 访问应用

启动成功后，在浏览器中访问：

```
本机访问：http://localhost:5000
局域网访问：http://[UOS系统IP]:5000
PDF编辑器：http://[UOS系统IP]:5000/editor
```

**获取UOS系统IP：**
```bash
ip addr show | grep inet
# 或
hostname -I
```

---

## 🔧 常用管理命令

### 容器管理

```bash
# 启动
docker-compose start
# 或
docker start pdf-editor

# 停止
docker-compose stop
# 或
docker stop pdf-editor

# 重启
docker-compose restart
# 或
docker restart pdf-editor

# 查看日志
docker-compose logs -f
# 或
docker logs -f pdf-editor

# 查看状态
docker-compose ps
# 或
docker ps | grep pdf-editor
```

### 资源监控

```bash
# 查看资源使用
docker stats pdf-editor-app

# 查看容器详情
docker inspect pdf-editor-app
```

---

## 📊 与原始版对比

### 时间成本

| 操作 | 精简版 | 原始版 | 节省 |
|------|--------|--------|------|
| **传输** | 3-5分钟 | 15-20分钟 | 75% |
| **导入** | 1-2分钟 | 5-8分钟 | 70% |
| **总计** | 4-7分钟 | 20-28分钟 | 75% |

### 存储占用

| 项目 | 精简版 | 原始版 | 节省 |
|------|--------|--------|------|
| **Windows端** | 267MB | 1.26GB | 79% |
| **UOS端** | 280MB | 1.32GB | 79% |
| **总计** | 547MB | 2.58GB | 79% |

---

## ✅ 验证功能

启动后验证以下功能是否正常：

### 主页面功能
- [ ] PDF上传
- [ ] 去红头及公章
- [ ] 去公章
- [ ] 转图片后去红头及公章
- [ ] 转Word文档
- [ ] 去背景
- [ ] 图片转PDF
- [ ] PDF页面管理（删除、合并、拆分）

### PDF编辑器
- [ ] 文字添加
- [ ] 形状绘制
- [ ] 擦除工具
- [ ] 高亮显示
- [ ] 撤销/重做
- [ ] 缩放控制

---

## 🔍 故障排查

### 问题1：容器无法启动

```bash
# 查看详细日志
docker logs pdf-editor-app

# 检查端口占用
sudo netstat -tlnp | grep 5000
```

### 问题2：无法访问应用

```bash
# 检查容器状态
docker ps | grep pdf-editor

# 检查防火墙
sudo ufw status
sudo ufw allow 5000/tcp

# 测试本地连接
curl http://localhost:5000
```

### 问题3：镜像导入失败

```bash
# 检查文件完整性
ls -lh pdf-editor-uos-lean-arm64.tar

# 验证文件大小应为 267-268MB
# 如果大小不对，重新传输文件

# 清理后重新导入
docker rmi pdf-editor-uos:lean
docker load -i pdf-editor-uos-lean-arm64.tar
```

---

## 📝 docker-compose.yml 配置

**当前配置已针对精简版镜像优化：**

```yaml
version: '3.8'

services:
  pdf-editor:
    platform: linux/arm64
    image: pdf-editor-uos:lean  # 使用精简版镜像
    container_name: pdf-editor-app
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
      - TZ=Asia/Shanghai
    volumes:
      - ./uploads:/app/uploads
      - ./processed:/app/processed
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G
    healthcheck:
      test: ["CMD-SHELL", "python -c 'import requests; requests.get(\"http://localhost:5000/\", timeout=5)' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - pdf-network

networks:
  pdf-network:
    driver: bridge
```

---

## 🎯 部署检查清单

### 部署前
- [ ] 已传输 `pdf-editor-uos-lean-arm64.tar` 到UOS系统
- [ ] 已传输 `docker-compose.yml` 到UOS系统（可选）
- [ ] UOS系统已安装Docker
- [ ] Docker服务已启动

### 部署中
- [ ] 成功导入镜像（280MB）
- [ ] 镜像标签为 `pdf-editor-uos:lean`
- [ ] 容器成功启动
- [ ] 端口5000已开放

### 部署后
- [ ] 可以通过浏览器访问应用
- [ ] 主要功能正常工作
- [ ] 容器设置为自动重启
- [ ] 数据卷正确挂载

---

## 💡 优化建议

### 1. 设置开机自启

```bash
# Docker服务自启
sudo systemctl enable docker

# 容器已配置 restart: unless-stopped
# 会在系统重启后自动启动
```

### 2. 备份数据

```bash
# 定期备份上传和处理的文件
tar -czf pdf-data-backup-$(date +%Y%m%d).tar.gz uploads processed
```

### 3. 监控资源

```bash
# 实时监控
watch -n 2 docker stats pdf-editor-app

# 查看历史日志
docker logs --since 24h pdf-editor-app
```

---

## 📦 文件说明

### 必需文件
- **pdf-editor-uos-lean-arm64.tar** (267MB)
  - 精简版Docker镜像
  - 功能完整
  - 已优化大小

### 可选文件
- **docker-compose.yml** (2KB)
  - 容器编排配置
  - 方便管理
  - 推荐使用

- **镜像优化结果.md**
  - 详细的优化报告
  - 大小对比分析

---

## 🔗 相关文档

- **UOS部署指南.md** - 完整的部署文档
- **镜像优化结果.md** - 优化详情和对比
- **镜像精简指南.md** - 精简方案说明
- **DOCKER_README.md** - Docker使用手册

---

## 📞 技术支持

### 常见问题

**Q: 精简版功能会受限吗？**  
A: 不会，功能完全一致，只是优化了镜像大小。

**Q: 可以从原始版升级到精简版吗？**  
A: 可以，停止旧容器，导入精简版镜像，重新启动即可。

**Q: 精简版性能如何？**  
A: 性能完全相同，只是减少了不必要的文件。

**Q: 如何验证镜像完整性？**  
A: 检查文件大小为267MB左右，导入后镜像大小为280MB。

---

## ✨ 总结

### 为什么选择精简版？

1. ✅ **节省时间** - 传输和导入快75%
2. ✅ **节省空间** - 占用减少79%
3. ✅ **功能完整** - 与原版完全一致
4. ✅ **性能相同** - 运行效率无差异
5. ✅ **易于管理** - 文件小，维护方便

### 快速上手

```bash
# 1. 导入镜像
docker load -i pdf-editor-uos-lean-arm64.tar

# 2. 启动应用
docker-compose up -d

# 3. 访问应用
http://[UOS-IP]:5000
```

---

**文档版本：** 1.0  
**更新时间：** 2024-12-12  
**适用镜像：** pdf-editor-uos:lean (280MB)  
**目标系统：** 统信UOS (arm64)  
**部署时长：** 4-7分钟（从传输到启动）
