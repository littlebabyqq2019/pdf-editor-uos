# 更新日志

## v1.2.4 (2026-08-02)

### 问题修复

- **Docker 配置文件持久化**：水印预设、全局水印配置文件由容器内 `/app/` 目录迁移至 `/app/config/`，支持挂载宿主机目录持久化保存，容器重启或重建后配置不再丢失

---

## v1.2.3 (2026-08-02)

### 新增功能

- **"OFD转PDF"按钮**
  - 支持将上传的OFD文件批量转换为PDF
  - 单个文件直接下载转换后的PDF，多个文件自动打包为 `OFD转PDF.zip`
  - 基于 `ofd2img` 库实现转换
  - 文件上传支持识别 `.ofd` 扩展名

### 问题修复

- "处理+水印"组合功能：仅生成1个文件时不再强制打包zip，直接下载该文件
- 添加水印、PDF转图片下载完成后自动清空文件列表，避免误重复处理

### 界面优化

- 水印默认字号由30调整为20，缩小默认水印视觉大小

---

## v1.2.2 (2026-07-31)

### 新增功能

- **"PDF转图片"按钮**
  - 将上传的PDF批量转换为JPG图片
  - 命名规则：单页PDF → `原文件名.jpg`；多页PDF → `原文件名-页面1.jpg`、`原文件名-页面2.jpg`……
  - 单个文件且只有1页：直接下载该 `.jpg`
  - 单个文件多页：图片放入以原文件名命名的文件夹，打包下载
  - 多个文件：每个文件的图片各自放入对应文件夹，统一打包为 `PDF转图片.zip` 下载
  - 使用 PyMuPDF 以 2 倍分辨率渲染，保证图片清晰度

### 问题修复

- 修复图片文件添加水印后字体明显偏小的问题
  - 根因：PDF 水印以 point（72 DPI 基准）为单位绘制，图片水印却直接按像素粘贴，图片分辨率越高水印相对越小
  - 修复：图片水印按图片尺寸与标准 A4 页面（595×842 点）的比例动态缩放，使视觉大小与 PDF 水印一致
- 修复图片添加水印后下载报"文件不存在"的问题（下载前误清空了刚生成的输出目录，调整为下载完成后再清空）
- 修复图片文件在"添加水印"功能中因类型标记不一致（`image` vs `图片`）而被误判为不支持类型的问题
- 单文件单水印场景不再强制打包压缩包，直接下载对应文件

### 界面优化

- 去掉水印选择弹窗中的提示文字（文件数量提示、勾选说明、格式说明），弹窗更简洁
- "添加水印"按钮新增支持图片文件（jpg/png/bmp/gif/tiff等），处理与命名逻辑与PDF一致，图片水印后保持原图片格式（不再转为PDF）
- 第五排"添加水印"与"PDF转图片"两个按钮配色统一为同一紫色系

---

## v1.2.1 (2026-07-31)

### 功能增强

- **多文件批量处理**
  - "添加水印"按钮现支持一次处理多个PDF文件
  - "处理+水印"组合按钮（去红头及公章+水印、去公章+水印、转图片后去红头及公章+水印）现支持批量处理多个文件
  - 每个原文件独立处理，生成的加水印文件存放在以原文件命名的文件夹中（`加水印-原文件名`）
  - 所有文件夹统一打包为 `已处理.zip` 下载

### 处理逻辑优化

- 文件夹结构：每个原文件对应一个文件夹，文件夹内包含应用不同水印预设后的多个PDF
- 命名规则：
  - 纯水印功能：`水印名-原文件名.pdf`
  - 组合功能：`水印名-操作前缀-原文件名.pdf`（如：医政科-去红头-文件1.pdf）
- 下载文件统一命名为 `已处理.zip`

---

## v1.2 (2026-07-30)

### 新增功能

- **PDF 水印功能**
  - 新增"水印设置"页面：全局配置字号、颜色、透明度、旋转角度、密度
  - 支持添加多个水印预设，仅需填写预设名称与水印文字
  - 新增"添加水印"按钮，可勾选一个或多个预设批量生成加水印后的 PDF
  - 水印采用图片方式嵌入（而非文本对象），复制/选中 PDF 正文时不会带出水印文字
  - 水印配置与预设保存在服务端（`watermark_config.json` / `watermark_presets.json`），不同电脑访问同一服务时自动共享同一套配置

- **"处理 + 水印"组合按钮**
  - 新增"PDF去红头及公章+水印"、"PDF去公章+水印"、"PDF转图片后去红头及公章+水印" 三个组合按钮
  - 点击后先执行对应的处理操作，再对结果批量添加所选水印，一步完成
  - 单文件直接下载为 `水印名-操作前缀-原文件名.pdf`；多文件打包为 `操作前缀-原文件名.zip`

### 问题修复

- 修复"转图片后去红头及公章+水印"生成的水印字体明显偏小的问题
  - 根因：`_convert_text_to_image_pdf`、`_remove_header_seal_scan_pdf`、`_remove_seal_scan_pdf` 三处将 PyMuPDF 渲染出的**像素尺寸**误当作 PDF 页面的**point 尺寸**创建新页面，导致图片型 PDF 页面被渲染倍率（1.5×）逐级放大，叠加后最终页面比原始 A4 放大约 2.25 倍
  - 修复：统一改为使用原始页面的真实 point 尺寸创建新页面，图片自动缩放填入，不影响清晰度
- 修复水印文字旋转方向偏差约 90° 的问题（PIL 图像坐标系与 PDF 坐标系方向差异导致，已通过实际渲染验证角度）
- 修复"添加水印"按钮点击后不弹出预设选择框的问题（文件类型判断逻辑与实际返回值不匹配）
- 修复"转图片后去红头及公章+水印"等组合按钮提示"没有可处理的PDF文件"的问题（前置操作的文件类型未正确识别为文本型/扫描型 PDF）

### 界面优化

- 水印预设选择框排列方式调整为 5 列，勾选后仅显示预设名称（不再显示水印文字内容）
- 下载的水印压缩包文件名改为"原文件名.zip"，不再使用固定名称
- 调整功能按钮排列顺序："添加水印"移至最后一排第一个，其后的"删除PDF页面/合并PDF文件/拆分PDF文件"依次上移一排
- 重命名部分功能按钮，统一加上"PDF"前缀以便区分：
  - 去红头及公章 → PDF去红头及公章
  - 去公章 → PDF去公章
  - 转图片后去红头及公章 → PDF转图片后去红头及公章
  - 去背景 → PDF去背景杂色
  - 转Word → 文本型PDF转Word
- "添加水印"按钮配色由红色调整为与其他水印相关按钮一致的紫色系，减少视觉突兀感

---

## v1.1 (2026-01-02)

- WPS 公章修复版：修复 WPS 中公章显示问题
